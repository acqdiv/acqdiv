[corpus]
iso639-3 == jpn
glottolog_code == nucl1643
format == JSON
language == Japanese
corpus == Japanese_MiiPro
owner == Yas Shirai

[paths]
sessions == ../corpora/Japanese_MiiPro/json/*.json
sessions_dir == ../corpora/Japanese_MiiPro/json/
metadata_dir == ../corpora/Japanese_MiiPro/xml/

[metadata]
type == XML
cdc == yes

[utterance]
type == orthographic

[speaker_labels]
birthday == birthdate
sex == gender_raw
id == speaker_label
language == languages_spoken
role == role_raw
name == name
age == age_raw

[session_labels]
Date == date
Id == source_id
Corpus == corpus
Lang == language
Media == media
Mediatypes == media_type

[json_mappings_utterance]
utterance_id == utterance_id
sentence_type == sentence_type
warnings == warning
speaker_id == speaker_label
addressee == addressee
starts_at == start_raw
ends_at == end_raw
comments == comment

[json_mappings_words]
full_word == word_actual
full_word_target == word_target
warnings == warning
# assign the proper actual vs target "word" given the corpus
word == word_actual

[morphemes]
type == actual

[json_mappings_morphemes]
segments == morpheme
glosses == gloss_raw
pos == pos_raw

[gloss]
MIIPRO == GOLD
PRES == PRS
CONN == CVB
PAST == PST
causal == because
POL == POL
SGER == NMLZ
DESID == DESID
NEG&OBL == NEG.OBLI
CONN&wa == CVB.TOP
NEG:n&PRES == NEG.PRS
NEG&IMP:de == NEG.IMP
POT:contr == POT
CAUS:contr == CAUS
CONC == CONC
ALT == ALT
OBJVZ == VBLZ
POL&IMP == POL.IMP
NEG&CONN == NEG.CVB
sugi&SGER == too.much.NMLZ
COND:contr == COND
ONN == CVB
COMP == CHANGE
PASt == PST
HORt == HORT
CAUS&SGER == CAUS.NMLZ

[regex]
