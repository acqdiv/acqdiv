[corpus]
iso639-3 == yua
glottolog_code == yuca1254
format == cha
language == Yucatec
corpus == Yucatec
owner == Barbara Pfeiler

[paths]
path == corpora
sessions == ${path}/${corpus:corpus}/${corpus:format}/*.${corpus:format}
sessions_dir == ${path}/${corpus:corpus}/${corpus:format}/
metadata_dir == ${path}/${corpus:corpus}/${metadata:type}/

[metadata]
type == cha
cdc == no
consistent_actual_target == no

[morphemes]
type == target
substitutions == no
