[corpus]
iso639-3 == tur
glottolog_code == nucl1301
format == cha
language == Turkish
corpus == Turkish
owner == Aylin Kuentay
acronym == KULLDD
name == Koç University Longitudinal Language Development Database

[paths]
path == corpora
sessions == ${path}/${corpus:corpus}/${corpus:format}/*.${corpus:format}
sessions_dir == ${path}/${corpus:corpus}/${corpus:format}/
metadata_dir == ${path}/${corpus:corpus}/${metadata:type}/

[metadata]
type == cha
