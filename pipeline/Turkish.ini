[corpus]
iso639-3 == tur
glottolog_code == nucl1301
format == JSON
language == Turkish
corpus == Turkish_KULLD
owner == Aylin Kuentay

[paths]
sessions == ../corpora/Turkish_KULLD/json/*.json
sessions_dir == ../corpora/Turkish_KULLD/json/
metadata_dir == ../corpora/Turkish_KULLD/xml/

[metadata]
type == XML
cdc == no

### THIS IS METADATA

[utterance]
# <w> == transcription
type == orthographic

[speaker_labels]
birthday == birthdate
sex == gender_raw
id == speaker_label
language == languages_spoken
role == role_raw
name == name
age == age_raw

[session_labels]
Date == date
Id == transcript_id
Corpus == corpus
Lang == language
Media == media
Mediatypes == media_type

[morphemes]
type == actual

[json_mappings_utterance]
utterance_id == utterance_id
speaker_id == speaker_label
starts_at == timestamp_start
ends_at == timestamp_end
english == translation
comments == comment
sentence_type == sentence_type

[json_mappings_words]
full_word == word_actual
full_word_target == word_target
warnings == warnings
# assign the proper actual vs target "word" given the corpus
word == word_actual

[json_mappings_morphemes]
segments == segment
segments_target == segment_target
glosses == gloss
glosses_target == gloss_target
pos == pos
pos_target == pos_target

[gloss]

[regex]
