[corpus]
iso639-3 == ctn
glottolog_code == chhi1245
format == toolbox
language == Chintang
corpus == Chintang
owner == Sabine Stoll, Elena Lieven
acronym == CLC
name == Chintang Language Corpus (Language Acquisition subcorpus)

[paths]
path == corpora
sessions == ${path}/${corpus:corpus}/${corpus:format}/*.txt
sessions_dir == ${path}/${corpus:corpus}/${corpus:format}/
metadata_dir == ${path}/${corpus:corpus}/${metadata:type}/

[metadata]
type == imdi
