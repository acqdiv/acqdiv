[corpus]
iso639-3 = ctn
glottolog_code = chhi1245
format = Toolbox
language = Chintang
corpus = CLDP

[paths]
sessions = ../../corpora/Chintang/toolbox/*.txt
sessions_dir = ../../corpora/Chintang/toolbox/
metadata_dir = ../../corpora/Chintang/metadata/
# src = %(src_dir)s/Chintang/toolbox

[metadata]
type = IMDI
cdc = no

[record_tiers]
record_marker = \ref
ref = utterance_id
ELANBegin = timestamp_start
ELANEnd = timestamp_end
tx = u_phonetic
eng = translation
ELANParticipant = speaker_id
gw = word
mph = morpheme
mgl = gloss
ps = pos

# [record_tiers]
# record_marker = \ref
# utterance_id = \ref
# timestamp_start = \ELANBegin
# timestamp_end = \ELANEnd
# u_phonetic = \tx
# translation = \eng
# speaker_id = \ELANParticipant
# word = \gw
# morpheme = \mph
# gloss = \mgl
# pos = \ps

# \nep = Nepali
# \com = Comment
# \comm = Comment
# \comment = Comment
# \Comment = Comment
# \context = Comment
# \cxt = Comment
# \eth  = Comment
# \gram = Comment



### THIS IS METADATA

[speaker_labels]
# this is a mapping from Speaker.fields to Chintang-IMDI-specific category labels
gender = sex
label = code
age = age
role = role
# birthdate = birthdate # TODO: this key is missing from come of the Chintang metadata; fails on processing
name = name
# SpeakerAgeInDays # TODO in post-processing?

[session_labels]
# WORK IN PROGRESS
transcript_id = id
date = date
genre = genre
situation = situation
# address = location.address
# continent = location.continent # here we refer to [location] section or unpack values in the metadata parser?
# country = location.country # here we refer to [location] section or unpack values in the metadata parser?


[functions]
# we probably need to define corpus-specific processing functions / classes and put them in the config
# otherwise we'll still end up stuck with a huge network of branches
# (not to mention a hardcoded selection of corpora, ew)
u_parsing = ulib.parse_chintang_utterance
